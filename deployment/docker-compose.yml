version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ingames_db
      POSTGRES_USER: ingames_user
      POSTGRES_PASSWORD: secret_password

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
