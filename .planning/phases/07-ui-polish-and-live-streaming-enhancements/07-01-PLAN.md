# Plan 07-01: UI Polish & Live Streaming Enhancements Implementation

**Phase:** 7 (UI Polish & Live Streaming Enhancements)
**Plan:** 07-01
**Mode:** tracer-first

## Goal
Implement animated Compose shimmer placeholders, exponential backoff WebSocket reconnection in `RealtimeClient`, button haptics, and win celebration overlays.

## Requirements Addressed
- **UIPOLISH-01**: Shimmer loading skeleton placeholders on `HomeScreen`.
- **UIPOLISH-02**: Offline reconnection banner & exponential backoff retry for `RealtimeClient`.
- **UIPOLISH-03**: Tactile haptic feedback on bet placement & cashout buttons.
- **UIPOLISH-04**: Win celebration overlays & live countdown tickers.

## Tasks

### Task 1: Shimmer Loading Modifier (`Shimmer.kt`)
- **Files**:
  - `android/app/src/main/kotlin/com/ingames/app/ui/components/Shimmer.kt`
- **Action**: Implement Compose `shimmerBackground()` modifier using animated linear gradient transition. Add skeleton cards to `HomeScreen.kt`.
- **Verification**: Compose screen renders shimmer while loading.

### Task 2: WebSocket Reconnection State (`RealtimeClient.kt`)
- **Files**:
  - `android/app/src/main/kotlin/com/ingames/app/data/RealtimeClient.kt`
- **Action**: Add `ConnectionState` flow (`CONNECTED`, `RECONNECTING`, `DISCONNECTED`) and exponential backoff retry logic.
- **Verification**: Unit test asserting retry attempts on connection drop.

### Task 3: Button Haptics & Win Overlays
- **Files**:
  - `android/app/src/main/kotlin/com/ingames/app/ui/screens/SevenUpDownGameScreen.kt`
  - `android/app/src/main/kotlin/com/ingames/app/ui/screens/CrushGameScreen.kt`
- **Action**: Add `LocalHapticFeedback` to bet buttons and win celebration overlay on win WebSocket events.
- **Verification**: Build & unit test check via `./gradlew :android:app:test`.

## Verification Plan

### Automated Verification
```bash
./gradlew :android:app:test
./gradlew test
```
